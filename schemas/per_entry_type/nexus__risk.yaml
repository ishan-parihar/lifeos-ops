# Per-Entry-Type Schema: nexus.Risk
# Auto-generated by scripts/upgrade_v0.9.0/generate_per_entry_type_schemas.py
# Documentation: Operational risk entry-type
# =============================================================================

schema_version: 0.9.0
schema_type: per_entry_type
applies_to_db: nexus
applies_to_entry_type: Risk
inherits_from:
- universal/holon_coordinate.yaml
- per_db/nexus.yaml
documentation_ref: Operational risk entry-type
properties:
  risk_probability:
    notion_type: number
    required: false
    description: 0-1 probability of materialization.
  risk_impact:
    notion_type: number
    required: false
    description: 0-10 impact if materialized.
  risk_horizon:
    notion_type: select
    required: false
    options:
    - immediate
    - days
    - weeks
    - months
    - quarters
    - years
  mitigation_strategy:
    notion_type: rich_text
    required: false
validation_rules:
- id: risk_kind_constraint
  description: Risk entries must have Kind=Catalyst (anticipated perturbation).
  rule: assert entry.kind == 'Catalyst'
