# Per-Entry-Type Schema: nexus.Decision
# Auto-generated by scripts/upgrade_v0.9.0/generate_per_entry_type_schemas.py
# Documentation: RELOCATED FROM GreatWay per Flag 3 (Decisions are Choice-class currency)
# =============================================================================

schema_version: 0.9.0
schema_type: per_entry_type
applies_to_db: nexus
applies_to_entry_type: Decision
inherits_from:
- universal/holon_coordinate.yaml
- per_db/nexus.yaml
documentation_ref: RELOCATED FROM GreatWay per Flag 3 (Decisions are Choice-class currency)
properties:
  choice_polarity:
    notion_type: select
    required: false
    description: STO/STS polarity of an emitted Choice (HoloOS doc 02.4 §2.3).
    options:
    - STO
    - STS
    - neutral
  crystallization_ratio:
    notion_type: number
    required: false
    description: 'Per HoloOS doc holon_states.yaml greater_cycle.preconditions. Min 0.7 required for `choice-locked` greater-cycle
      phase. Range: 0.0-1.0.'
  decision_polarity:
    notion_type: select
    required: false
    options:
    - STO
    - STS
    - neutral
  decision_scope:
    notion_type: select
    required: false
    options:
    - personal
    - relational
    - operational
    - strategic
    - epochal
  decision_irreversibility:
    notion_type: select
    required: false
    options:
    - reversible
    - partially_reversible
    - irreversible
    - existential
  decided_at:
    notion_type: date
    required: false
validation_rules:
- id: decision_kind_constraint
  description: Decision entries must have Kind=Choice (decisions are micro-Choices emitted through the contact boundary).
  rule: assert entry.kind == 'Choice'
