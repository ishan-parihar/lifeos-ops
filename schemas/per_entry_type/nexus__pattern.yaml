# Per-Entry-Type Schema: nexus.Pattern
# Auto-generated by scripts/upgrade_v0.9.0/generate_per_entry_type_schemas.py
# Documentation: 04.2.1_Mind_Complex_Architecture.md §4 (archetype 6 Transformation of Mind)
# =============================================================================

schema_version: 0.9.0
schema_type: per_entry_type
applies_to_db: nexus
applies_to_entry_type: Pattern
inherits_from:
- universal/holon_coordinate.yaml
- per_db/nexus.yaml
documentation_ref: 04.2.1_Mind_Complex_Architecture.md §4 (archetype 6 Transformation of Mind)
properties:
  crucible_intensity:
    notion_type: select
    required: false
    description: Per HoloOS doc holon_states.yaml greater_cycle.preconditions.
    options:
    - moderate
    - acute
  transformation_threshold:
    notion_type: number
    required: false
    description: 'T_thresh — the threshold the Significator must reach for this Transformation-kind Nexus entry to fire (HoloOS
      doc 03.1 §3 stage 8). Default: 110.0 (from lifeos.config.default.json nexus_firing.pressure_threshold).'
  pattern_recurrence_count:
    notion_type: number
    required: false
    description: Number of times this pattern has recurred.
  pattern_recurrence_interval:
    notion_type: select
    required: false
    options:
    - daily
    - weekly
    - monthly
    - quarterly
    - annual
    - irregular
  pattern_archetypal_resonance:
    notion_type: rich_text
    required: false
    description: Which of the 22 named archetypes this pattern most resonates with.
archetype_number: 6
validation_rules:
- id: pattern_kind_constraint
  description: Pattern entries must have Kind=Transformation.
  rule: assert entry.kind == 'Transformation'
