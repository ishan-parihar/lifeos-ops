# Per-Entry-Type Schema: nexus.Note
# Auto-generated by scripts/upgrade_v0.9.0/generate_per_entry_type_schemas.py
# Documentation: RELOCATED FROM Potentiator (raw notes are Catalyst-class currency per HoloOS doc 02.1 §1)
# =============================================================================

schema_version: 0.9.0
schema_type: per_entry_type
applies_to_db: nexus
applies_to_entry_type: Note
inherits_from:
- universal/holon_coordinate.yaml
- per_db/nexus.yaml
documentation_ref: RELOCATED FROM Potentiator (raw notes are Catalyst-class currency per HoloOS doc 02.1 §1)
properties:
  synthesis_state:
    notion_type: select
    required: false
    options:
    - raw_note
    - annotated
    - synthesized
    - applied
    description: Tracks the 9-stage digestion state of this note.
  source_url:
    notion_type: url
    required: false
    description: Original URL where this note was sourced.
  capture_method:
    notion_type: select
    required: false
    options:
    - manual
    - web_clipper
    - api_ingest
    - voice_memo
    - import
  raw_content:
    notion_type: rich_text
    required: false
    description: Original raw note content (before processing).
  highlight_count:
    notion_type: number
    required: false
    description: Number of highlights extracted.
validation_rules:
- id: note_kind_constraint
  description: Note entries must have Kind=Catalyst (raw notes are unprocessed perturbations).
  rule: assert entry.kind == 'Catalyst'
