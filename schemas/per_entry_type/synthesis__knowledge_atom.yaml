# Per-Entry-Type Schema: synthesis.Knowledge-Atom
# Auto-generated by scripts/upgrade_v0.9.0/generate_per_entry_type_schemas.py
# Documentation: RELOCATED FROM Potentiator (single synthesized knowledge units)
# =============================================================================

schema_version: 0.9.0
schema_type: per_entry_type
applies_to_db: synthesis
applies_to_entry_type: Knowledge-Atom
inherits_from:
- universal/holon_coordinate.yaml
- per_db/synthesis.yaml
documentation_ref: RELOCATED FROM Potentiator (single synthesized knowledge units)
properties:
  synthesis_state:
    notion_type: select
    required: false
    description: Tracks the 9-stage digestion state of a note/knowledge atom as it crystallizes from Catalyst-class Note into
      Experience-class Knowledge into refined Catalyst.
    options:
    - raw_note
    - annotated
    - synthesized
    - applied
  atom_statement:
    notion_type: rich_text
    required: true
    description: The single synthesized knowledge statement.
  evidence_strength:
    notion_type: select
    required: false
    options:
    - anecdotal
    - case_study
    - correlational
    - causal
    - axiomatic
  confidence_score:
    notion_type: number
    required: false
    description: 0-1 confidence in this knowledge atom.
  source_count:
    notion_type: number
    required: false
    description: Number of independent sources corroborating this atom.
validation_rules:
- id: knowledge_atom_kind_constraint
  description: Knowledge-Atom entries must have Kind=Experience.
  rule: assert entry.kind == 'Experience'
