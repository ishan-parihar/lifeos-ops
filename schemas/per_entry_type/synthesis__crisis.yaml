# Per-Entry-Type Schema: synthesis.Crisis
# Auto-generated by scripts/upgrade_v0.9.0/generate_per_entry_type_schemas.py
# Documentation: Operational crisis entry-type (HoloOS doc 03.1 §3 stage 8)
# =============================================================================

schema_version: 0.9.0
schema_type: per_entry_type
applies_to_db: synthesis
applies_to_entry_type: Crisis
inherits_from:
- universal/holon_coordinate.yaml
- per_db/synthesis.yaml
documentation_ref: Operational crisis entry-type (HoloOS doc 03.1 §3 stage 8)
properties:
  crucible_intensity:
    notion_type: select
    required: false
    description: Per HoloOS doc holon_states.yaml greater_cycle.preconditions.
    options:
    - moderate
    - acute
  transformation_threshold:
    notion_type: number
    required: false
    description: 'T_thresh — the threshold the Significator must reach for this Transformation-kind Nexus entry to fire (HoloOS
      doc 03.1 §3 stage 8). Default: 110.0 (from lifeos.config.default.json synthesis_firing.pressure_threshold).'
  crisis_intensity:
    notion_type: select
    required: false
    options:
    - low
    - moderate
    - acute
    - existential
  crisis_domain:
    notion_type: select
    required: false
    options:
    - health
    - relational
    - financial
    - professional
    - existential
    - civilizational
  crisis_started_at:
    notion_type: date
    required: false
  crisis_resolved_at:
    notion_type: date
    required: false
  resolution_path:
    notion_type: rich_text
    required: false
validation_rules:
- id: crisis_kind_constraint
  description: Crisis entries must have Kind=Transformation (acute threshold events).
  rule: assert entry.kind == 'Transformation'
