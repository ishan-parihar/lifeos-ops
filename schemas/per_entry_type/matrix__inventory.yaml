# Per-Entry-Type Schema: matrix.Inventory
# Auto-generated by scripts/upgrade_v0.9.0/generate_per_entry_type_schemas.py
# Documentation: Operational inventory entry-type
# =============================================================================

schema_version: 0.9.0
schema_type: per_entry_type
applies_to_db: matrix
applies_to_entry_type: Inventory
inherits_from:
- universal/holon_coordinate.yaml
- per_db/matrix.yaml
documentation_ref: Operational inventory entry-type
properties:
  inventory_class:
    notion_type: select
    required: false
    description: Class of inventory (resource / health / asset / capability).
    options:
    - resource
    - health
    - asset
    - capability
    - social_capital
  current_count:
    notion_type: number
    required: false
    description: Current count/level.
  unit:
    notion_type: select
    required: false
    description: Unit of measure.
    options:
    - count
    - hours
    - currency
    - kg
    - percent
    - score
