# Per-Entry-Type Schema: logbook.Diet
# Auto-generated by scripts/upgrade_v0.9.0/generate_per_entry_type_schemas.py
# Documentation: Per Flag 1: Diet is Catalyst-class currency
# =============================================================================

schema_version: 0.9.0
schema_type: per_entry_type
applies_to_db: logbook
applies_to_entry_type: Diet
inherits_from:
- universal/holon_coordinate.yaml
- per_db/logbook.yaml
documentation_ref: 'Per Flag 1: Diet is Catalyst-class currency'
properties:
  diet_class:
    notion_type: select
    required: false
    options:
    - food
    - information
    - media
    - social
    - somatic
  caloric_load:
    notion_type: number
    required: false
  digestibility_score:
    notion_type: number
    required: false
    description: 0-1 ease of metabolic integration.
validation_rules:
- id: diet_must_be_catalyst_role
  description: 'Per UPGRADE_FLAGS.md Flag 1: Diet entries must have archetype_role=Catalyst.'
  rule: assert entry.archetype_role == 'Catalyst'
