# Per-Entry-Type Schema: synthesis.Transformation-Event
# Auto-generated by scripts/upgrade_v0.9.0/generate_per_entry_type_schemas.py
# Documentation: Operational transformation-event entry-type
# =============================================================================

schema_version: 0.9.0
schema_type: per_entry_type
applies_to_db: synthesis
applies_to_entry_type: Transformation-Event
inherits_from:
- universal/holon_coordinate.yaml
- per_db/synthesis.yaml
documentation_ref: Operational transformation-event entry-type
properties:
  crucible_intensity:
    notion_type: select
    required: false
    description: Per HoloOS doc holon_states.yaml greater_cycle.preconditions.
    options:
    - moderate
    - acute
  transformation_threshold:
    notion_type: number
    required: false
    description: 'T_thresh — the threshold the Significator must reach for this Transformation-kind Nexus entry to fire (HoloOS
      doc 03.1 §3 stage 8). Default: 110.0 (from lifeos.config.default.json synthesis_firing.pressure_threshold).'
  transformation_magnitude:
    notion_type: select
    required: false
    options:
    - micro
    - minor
    - moderate
    - major
    - epochal
  transformation_axis:
    notion_type: select
    required: false
    options:
    - mind
    - body
    - spirit
    - relational
    - environmental
    - systemic
  transformation_triggered_at:
    notion_type: date
    required: false
  transformation_completed_at:
    notion_type: date
    required: false
  pre_transformation_signature:
    notion_type: rich_text
    required: false
    description: Valence Signature YAML BEFORE transformation.
  post_transformation_signature:
    notion_type: rich_text
    required: false
    description: Valence Signature YAML AFTER transformation.
validation_rules:
- id: transformation_event_kind_constraint
  description: Transformation-Event entries must have Kind=Transformation.
  rule: assert entry.kind == 'Transformation'
