# Per-Entry-Type Schema: context.Place
# Auto-generated by scripts/upgrade_v0.9.0/generate_per_entry_type_schemas.py
# Documentation: NEW entry-type (geographic operating environment per HoloOS doc type_codes T12)
# =============================================================================

schema_version: 0.9.0
schema_type: per_entry_type
applies_to_db: context
applies_to_entry_type: Place
inherits_from:
- universal/holon_coordinate.yaml
- per_db/context.yaml
documentation_ref: NEW entry-type (geographic operating environment per HoloOS doc type_codes T12)
properties:
  quadrant:
    notion_type: select
    required: false
    description: AQAL quadrant (HoloOS doc holon_type_placement.yaml).
    options:
    - UL
    - UR
    - LL
    - LR
  dimension:
    notion_type: select
    required: false
    description: Interior/Exterior dimension (HoloOS doc holon_type_placement.yaml).
    options:
    - Interior
    - Exterior
  place_type:
    notion_type: select
    required: false
    options:
    - home
    - workplace
    - city
    - region
    - country
    - biome
    - sacred_site
    - virtual
  place_latitude:
    notion_type: number
    required: false
  place_longitude:
    notion_type: number
    required: false
  place_resonance_score:
    notion_type: number
    required: false
    description: 0-1 resonance with the focal holon's Significator.
validation_rules:
- id: place_quadrant_required
  description: 'Per HoloOS doc holon_type_placement.yaml: Place entries must have quadrant=LR.'
  rule: assert entry.quadrant == 'LR'
