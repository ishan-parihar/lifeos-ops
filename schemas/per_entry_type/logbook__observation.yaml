# Per-Entry-Type Schema: logbook.Observation
# Auto-generated by scripts/upgrade_v0.9.0/generate_per_entry_type_schemas.py
# Documentation: Per Flag 1: Observation is Catalyst-class currency
# =============================================================================

schema_version: 0.9.0
schema_type: per_entry_type
applies_to_db: logbook
applies_to_entry_type: Observation
inherits_from:
- universal/holon_coordinate.yaml
- per_db/logbook.yaml
documentation_ref: 'Per Flag 1: Observation is Catalyst-class currency'
properties:
  observation_class:
    notion_type: select
    required: false
    options:
    - empirical
    - intuitive
    - reported
    - inferred
    - remembered
  confidence_score:
    notion_type: number
    required: false
    description: 0-1 confidence in this observation.
  source_attribution:
    notion_type: rich_text
    required: false
    description: Where the observation came from.
validation_rules:
- id: observation_must_be_catalyst_role
  description: 'Per UPGRADE_FLAGS.md Flag 1: Observation entries must have archetype_role=Catalyst.'
  rule: assert entry.archetype_role == 'Catalyst'
