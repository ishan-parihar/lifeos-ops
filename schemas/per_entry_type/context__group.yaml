# Per-Entry-Type Schema: context.Group
# Auto-generated by scripts/upgrade_v0.9.0/generate_per_entry_type_schemas.py
# Documentation: RELOCATED FROM Significator (small collective holons)
# =============================================================================

schema_version: 0.9.0
schema_type: per_entry_type
applies_to_db: context
applies_to_entry_type: Group
inherits_from:
- universal/holon_coordinate.yaml
- per_db/context.yaml
documentation_ref: RELOCATED FROM Significator (small collective holons)
properties:
  quadrant:
    notion_type: select
    required: false
    description: AQAL quadrant (HoloOS doc holon_type_placement.yaml).
    options:
    - UL
    - UR
    - LL
    - LR
  dimension:
    notion_type: select
    required: false
    description: Interior/Exterior dimension (HoloOS doc holon_type_placement.yaml).
    options:
    - Interior
    - Exterior
  bonding_disposition:
    notion_type: select
    required: false
    description: 'Per HoloOS doc 02.3 §4: Bonding occurs at the Significator⇄Great-Way surface. 4 bonding types: ionic, covalent,
      dative, metallic.'
    options:
    - ionic
    - covalent
    - dative
    - metallic
  bonding_partner_count:
    notion_type: number
    required: false
    description: Number of active Significator-side bonds this GreatWay entry maintains. Used to compute metallic-bonding
      lattice density.
  group_size:
    notion_type: number
    required: false
    description: Number of members in the group.
  group_cohesion:
    notion_type: select
    required: false
    options:
    - fragmented
    - loose
    - coherent
    - tight
    - monolithic
  group_bonding_type:
    notion_type: select
    required: false
    description: Dominant bonding type within the group (HoloOS doc 02.3 §4).
    options:
    - ionic
    - covalent
    - dative
    - metallic
    - mixed
  group_purpose:
    notion_type: rich_text
    required: false
validation_rules:
- id: group_quadrant_required
  description: 'Per HoloOS doc holon_type_placement.yaml: Group entries must have quadrant=LL.'
  rule: assert entry.quadrant == 'LL'
