# Per-Entry-Type Schema: nexus.Knowledge-Category
# Auto-generated by scripts/upgrade_v0.9.0/generate_per_entry_type_schemas.py
# Documentation: RELOCATED FROM Potentiator (digested notes are Experience-class currency per HoloOS doc 02.1 §1)
# =============================================================================

schema_version: 0.9.0
schema_type: per_entry_type
applies_to_db: nexus
applies_to_entry_type: Knowledge-Category
inherits_from:
- universal/holon_coordinate.yaml
- per_db/nexus.yaml
documentation_ref: RELOCATED FROM Potentiator (digested notes are Experience-class currency per HoloOS doc 02.1 §1)
properties:
  category_scope:
    notion_type: select
    required: false
    options:
    - domain
    - discipline
    - methodology
    - framework
    - toolkit
  atom_count:
    notion_type: number
    required: false
    description: Number of Knowledge-Atoms in this category.
  maturity_score:
    notion_type: number
    required: false
    description: 0-1 maturity of the category (per HoloOS doc 03.1 §3 stages 5-6).
  applied_count:
    notion_type: number
    required: false
    description: Number of times this category has been applied in practice.
validation_rules:
- id: knowledge_category_kind_constraint
  description: Knowledge-Category entries must have Kind=Experience (digested, categorized knowledge).
  rule: assert entry.kind == 'Experience'
